[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"


[project]
name = "nearly"
version = "1.0.0"
readme = "README.md"

[tool.setuptools.packages.find]
where = ["."]
